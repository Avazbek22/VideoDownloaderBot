name: ${COMPOSE_PROJECT_NAME:-videodownloaderbot}

services:
  videodownloaderbot:
    image: videodownloaderbot:local
    build:
      context: .
    restart: unless-stopped
    init: true
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    read_only: true
    tmpfs:
      - /tmp:size=256m,mode=1777
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
